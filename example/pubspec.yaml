name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <4.0.0'
  flutter: '>=3.13.2'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  super_extensions:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

